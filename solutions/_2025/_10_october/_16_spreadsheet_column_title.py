"""
In many spreadsheet applications, the columns are marked with letters. From the
1st to the 26th column the letters are A to Z. Then starting from the 27th
column it uses `AA`, `AB`, ..., `ZZ`, `AAA`, etc.

Given a number `n`, find the `n`-th column name.
"""

# I've already done this problem.
from solutions._2025._10_october._13_spreadsheet_column_title import (
    convert_to_title_shorter as column_title,  # type: ignore
)
